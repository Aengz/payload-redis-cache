{
  "name": "payload-redis-cache",
  "description": "Redis cache plugin for Payload CMS",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "dev": "cd ./dev && yarn dev",
    "build": "tsc",
    "dev:build": "yarn build && yarn unlink && yarn link && cd dev && yarn unlink payload-redis-cache && yarn link --force payload-redis-cache"
  },
  "peerDependencies": {
    "payload": "^1.3.0",
    "redis": "^4.5.1"
  },
  "files": [
    "dist",
    "*.js",
    "*.d.ts",
    "!.prettierrc.js"
  ],
  "devDependencies": {
    "@types/express": "^4.17.9",
    "copyfiles": "^2.4.1",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.6",
    "payload": "^1.3.0",
    "redis": "^4.5.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.9.4"
  }
}
